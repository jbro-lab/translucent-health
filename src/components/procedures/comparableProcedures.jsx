// const handleCardClick = () => {
//   const id = procedure.id
//   const ProcedureSearchParams = new URLSearchParams({ id });
//   const url = `procedure?${ProcedureSearchParams.toString()}`;
//   window.location.href = url;
// };

//Still need to write this component. Then render it somewhere helpful on the page.
//It will show the prices for the same procedure at multiple hospitals within a geographic location. 